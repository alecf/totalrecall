# Xcode
build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcodeproj/project.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
xcuserdata/

# Swift Package Manager
.build/
Packages/
# Package.resolved IS committed — this is an app, not a library. It pins the
# exact Sparkle build that ships in the DMG so releases are reproducible from
# their tag, and Dependabot's swift updater reads it to find version bumps.

# macOS
.DS_Store
*.swp
*~
.Trashes

# Test fixtures captured from real machines (may contain secrets)
Fixtures/*.json

# Claude Code
.claude/settings.local.json

# Crash reports
*.ips
*.crash

# Site
site/node_modules/
site/dist/
.playwright-mcp/
