#!/usr/bin/env swift
// This is a standalone script that uses the same classification logic as the app.
// Run: swift tools/diagnose-groups.swift
// Or build: swift build && .build/debug/TotalRecallDiag

// NOTE: This won't work as a standalone script because it needs the module.
// Use the TotalRecallDiag target instead: swift run TotalRecallDiag
print("Use: swift run TotalRecallDiag")
